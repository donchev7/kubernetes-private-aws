apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jenkins-master-pvc
  labels:
    app: jenkins-master
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi